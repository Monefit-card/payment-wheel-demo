import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev-tools badge floats over the bottom-left of every screen, which
  // reads as page chrome when the demo is being presented.
  devIndicators: false,
};

export default nextConfig;
